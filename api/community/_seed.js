// Seed list, used only when the Blob store is still empty (the very first read
// after deploy). Once anything is written, Blob is the source of truth and
// edits here have no effect — add real entries through the admin panel.
export default [];
